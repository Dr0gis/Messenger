package team.corpore.`in`.messenger

import org.junit.Test
import team.corpore.`in`.messenger.domain.interactors.impl.WelcomingInteractorImpl



class WelcomeMessageUnitTest {

    @Test
    @Throws(Exception::class)
    fun testWelcomeMessageFound() {
        /*val msg = "Welcome, friend!"
        when (messageRepository.getWelcomeMessage())
            .thenReturn(msg)

        val interactor = WelcomingInteractorImpl(
            mExecutor,
            mMainThread,
            mMockedCallback,
            mMessageRepository
        )
        interactor.run()
        Mockito.verify(mMessageRepository).getWelcomeMessage()
        Mockito.verifyNoMoreInteractions(mMessageRepository)
        Mockito.verify(mMockedCallback).onMessageRetrieved(msg)*/
    }

}